export NVM_DIR="$HOME/.nvm"
[ -s "/opt/homebrew/opt/nvm/nvm.sh" ] && \. "/opt/homebrew/opt/nvm/nvm.sh"  # This loads nvm
[ -s "/opt/homebrew/opt/nvm/etc/bash_completion.d/nvm" ] && \. "/opt/homebrew/opt/nvm/etc/bash_completion.d/nvm"  # This loads nvm bash_completion
alias pip='python3 -m pip'
alias ll='ls -lha




==> Formulae
azure-cli		helm			krb5			libunistring		node			redis
brew-cask-completion	htop			kubelogin		libuv			nvm			sqlite
brotli			icu4c@76		kubernetes-cli		libyaml			openssl@3		watch
c-ares			icu4c@77		libidn2			lz4			postgresql@14		wget
ca-certificates		k6			libnghttp2		mpdecimal		python@3.12		xz
gettext			k9s			libsodium		ncurses			readline